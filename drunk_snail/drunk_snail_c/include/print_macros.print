#include <string.h>

#include "../modules/memoma/include/memoma.h"



render__empty {%
<(*other).start[:depth].left+><LINE><(*other).start[:depth].right->

%}

render__param {%
<(*other).start[:depth].left+><OTHER_LEFT><ARG><OTHER_RIGHT><(*other).start[:depth].right->

%}